//  task number 3

let users: string[] = ["Eric", "Robbert","Sara","Marry", "admin"];

for (var user of users)
if (users.length==0){ console.log( " we need to find some users ")} 
else {console.log(" Hello",user.toUpperCase())}
users.splice(0)
if (users.length==0){ console.log( " we need to find some users ")}




